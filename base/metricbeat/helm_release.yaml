apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: metricbeat
spec:
  releaseName: metricbeat
  chart:
    repository: "https://helm.elastic.co"
    name: metricbeat
    version: 7.6.0
  values:
